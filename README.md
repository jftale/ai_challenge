# SSAFY 16기 2회차 AI 챌린지 — 텍스트 이미지 기반 VQA 솔루션

이미지와 질문을 입력받아 `a / b / c / d` 중 하나를 고르는 Visual Question Answering 모델입니다.
베이스라인(v3)의 구조를 유지하면서, **점수에 실제로 영향이 큰 부분만 골라** 수정했습니다.

- 메인 코드: **[`vqa_qwen25vl_solution.ipynb`](./vqa_qwen25vl_solution.ipynb)** — 위에서부터 순서대로 실행하면 `submission.csv`가 생성됩니다.
- 로직 단위 테스트: [`tests/test_logic.py`](./tests/test_logic.py) — GPU 없이 `python tests/test_logic.py`로 검증 가능

---

## 1. 문제를 어떻게 이해했는가

대회 이름은 VQA지만, 예시 문항을 보면 성격이 분명합니다.

> 질문: 표지판은 어디로 향하는 길을 안내하고 있나요?
> 선지: a.역삼 b.선릉 c.사상 d.강남

> 질문: 사진 속 선반에 적힌 문구는 무엇인가요?
> 선지: a.SUNDAY BURGER LOVE b.MONDAY SALAD TIME …

이건 "장면을 이해하는" 문제가 아니라 **이미지 속 글자를 정확히 읽어내는(OCR) 문제**입니다.
대회 설명에도 "**텍스트가 포함된** 이미지 기반 질의응답"이라고 명시되어 있습니다.

> **따라서 최우선 과제는 "모델이 글자를 읽을 수 있게 만드는 것"** 이고,
> 그 다음이 "읽은 내용으로 4지선다를 고르는 것"입니다.
> 이 판단이 아래 모든 설계 결정의 근거입니다.

---

## 2. 베이스라인의 병목 진단

| 진단 | 근거 |
|---|---|
| **① 해상도가 치명적으로 낮다** | `max_pixels = 256×28×28` ≈ 448×448. 간판·메뉴판의 작은 글씨는 이 해상도에서 형체가 사라집니다. 아무리 학습해도 **읽을 수 없는 정보는 맞힐 수 없습니다.** |
| **② 파싱 실패가 곧 오답** | 생성 결과에서 `a~d`를 못 찾으면 `return "a"`. 모델이 `"정답은"`, 공백, 한글을 뱉는 순간 사실상 랜덤입니다. |
| **③ 라벨 마스킹 부재** | `labels = input_ids.clone()` → 시스템 프롬프트·질문·보기·이미지·**패딩까지 전부** 학습 대상. 정답 선택 능력은 거의 늘지 않고 질문 문장만 암기 → 과적합. |
| **④ 위치 편향 방치** | 언어모델은 "모르면 (a)" 경향이 있습니다. 보정 장치가 없습니다. |
| **⑤ 데이터를 버린다** | `train_df.sample(n=1000)`. 나머지를 쓰지 않을 이유가 없습니다. |
| **⑥ 검증 지표가 loss** | 4지선다에서 중요한 건 **정확도**입니다. loss가 내려가는데 정확도는 떨어지는 구간이 실제로 존재합니다. |
| **⑦ EXIF 회전 미처리** | 휴대폰 사진은 픽셀은 가로인데 메타데이터로만 세로인 경우가 많습니다. 글자가 누운 채 입력됩니다. |

---

## 3. 개선 내역과 근거

### 3.1 해상도 상향 — 가장 큰 단일 개선

해상도는 픽셀이 아니라 **비전 토큰 예산**(`*_max_tokens`)으로 지정합니다.

| 설정 | 토큰 예산 | 실질 해상도 |
|---|---|---|
| 베이스라인 | `256` | 약 448×448 |
| 본 솔루션 (추론) | `1024~1600` | 약 1000~1300px |

읽을 수 없던 글자가 읽히기 시작하므로, 다른 어떤 기법보다 효과가 큽니다.
GPU VRAM을 자동 감지해 감당 가능한 최대값을 고르고, OOM이 나면 배치를 1로 줄여 재시도합니다.

> **왜 픽셀이 아니라 토큰인가:** 토큰 1개가 담는 픽셀 수가 **모델마다 다릅니다.**
> Qwen2.5-VL은 `28×28`, Qwen3-VL은 `32×32`입니다.
> 픽셀 값을 하드코딩하면 모델을 교체하는 순간 해상도가 조용히 틀어집니다
> (같은 1280 토큰이라도 Qwen3는 Qwen2.5보다 **31% 많은 픽셀**을 봅니다).
> 그래서 환산 단위를 `MODEL_ZOO`의 `px_unit`에 두고 자동 처리합니다.

### 3.2 모델 상향 + 교체 가능 구조

기본값은 **Qwen3-VL-8B-Instruct**이며, `CFG["model_key"]` 한 줄로 교체됩니다.
`transformers`가 4.57 미만이면 자동으로 Qwen2.5-VL-7B로 내려갑니다.
4bit(NF4) 양자화로 VRAM 약 7GB이므로 T4(16GB)에서도 동작하고, 12GB 이하면 4B로 강등됩니다.

모델 교체가 실제로 동작하려면 세 가지가 모델에 비종속적이어야 하고, 전부 그렇게 구현했습니다.

| 항목 | 구현 |
|---|---|
| 모델 로드 | `AutoModelForImageTextToText` (Qwen2.5-VL / Qwen3-VL 공통) |
| 해상도 환산 | `MODEL_ZOO`의 `px_unit`으로 토큰→픽셀 자동 변환 |
| 라벨 마스킹 경계 | assistant 표식을 **chat template에서 직접 추출** (하드코딩 제거) |

마지막 항목이 특히 중요합니다. 표식을 못 찾으면 라벨이 전부 `-100`이 되어
**학습이 조용히 무의미해집니다.** 그래서 학습 시작 전에 `assert`로 강제 검사합니다
(학습 대상 토큰 수가 0개거나 8개를 넘으면 즉시 중단).

### 3.3 ★ 생성 → 로짓 스코어링

`assistant` 차례의 **바로 다음 토큰 확률분포**에서 `a`, `b`, `c`, `d` 토큰만 꺼내 비교합니다.
(대문자·공백 변형 토큰의 확률까지 `logsumexp`로 합산한 뒤 4개만 남겨 재정규화)

```
logits  →  log_softmax  →  [a,b,c,d] 추출  →  softmax  →  4지선다 확률
```

- **파싱 실패 0건** — 항상 4지선다 안에서만 고릅니다
- 생성 대신 토큰 1개 분량 계산이므로 **더 빠릅니다**
- **확신도(확률)** 가 남으므로 아래의 TTA·앙상블·보정에 그대로 쓸 수 있습니다

`generate(output_logits=True)`로 **후처리되지 않은 원본 로짓**을 받습니다.
(`scores`는 `repetition_penalty` 등이 적용되어 왜곡될 수 있어 `logits`를 우선 사용하고,
구버전 대비 `repetition_penalty=1.0`을 명시합니다.)

### 3.4 ★ 보기 순서 순환 치환 TTA

같은 문제를 보기 순서만 바꿔 4번 물어보고, **내용 기준으로** 확률을 평균냅니다.

```
1회차: (a)=1번보기 (b)=2번 (c)=3번 (d)=4번
2회차: (a)=2번보기 (b)=3번 (c)=4번 (d)=1번
3회차: (a)=3번보기 (b)=4번 (c)=1번 (d)=2번
4회차: (a)=4번보기 (b)=1번 (c)=2번 (d)=3번
```

각 보기가 a/b/c/d 자리에 **정확히 한 번씩** 오므로 위치 효과가 수학적으로 상쇄됩니다.
`tests/test_logic.py`에 위치 편향이 신호보다 강한(+0.45 vs +0.30) 상황에서도
TTA가 정답을 복원하고 TTA 없이는 오답이 나오는 것을 시뮬레이션으로 검증해 두었습니다.

### 3.5 ★ 라벨 마스킹

`<|im_start|>assistant\n` 토큰열을 **뒤에서부터** 탐색해 그 뒤(정답 글자 + 종료 토큰)만
학습 대상으로 남기고 나머지는 전부 `-100`으로 마스킹합니다. 패딩도 제외합니다.

실측(단위 테스트 기준) 예시: 베이스라인은 22개 토큰 전부를 학습, 본 솔루션은 6개만 학습.
학습 신호가 "정답 선택"에만 집중되고, 질문 암기로 인한 과적합이 사라집니다.

### 3.6 학습 데이터 확대 + 보기 셔플 증강

- `train.csv` **전체** 사용 (VRAM이 작은 프리셋만 상한 적용)
- 매 에폭 **보기 순서를 무작위로 섞고 정답 글자도 함께 변경** → 실질 4배 증강 + 위치 편향 학습 방지
- `dev.csv` 활용: 교육생 5명 응답의 **다수결**을 의사라벨로 사용.
  **5명 중 4명 이상 합의 + 합의율 80% 이상** 문항만 채택하고, 3:2 같은 애매한 건 **버립니다.**
  응답이 `"c"`, `"C"`, `"a) 역삼"`, `"3"`, `"김치찌개와 삼겹살"` 등 어떤 형태로 와도
  선지 본문 유사도 매칭(≥0.85)까지 동원해 `a~d`로 정규화합니다 (16개 케이스 테스트 통과).
  대회 규정상 dev 증강은 허용 범위입니다.

### 3.7 LoRA 적용 범위 정정

베이스라인의 `target_modules=["q_proj", ..., "gate_proj","up_proj","down_proj"]`는
**이름이 겹쳐 비전 타워의 MLP까지 의도치 않게 학습시킵니다**
(Qwen2.5-VL 비전 블록도 `gate_proj/up_proj/down_proj`를 씁니다).

본 솔루션은 모델의 모듈을 직접 스캔해 `visual` 경로를 제외한 **언어모델 쪽 전체 모듈명**만
명시적으로 넘깁니다. 학습 파라미터가 줄고 과적합 위험도 함께 줄어듭니다.

### 3.8 EXIF 회전 보정

`ImageOps.exif_transpose()` 한 줄. 휴대폰 사진에서 글자가 90° 누워 들어가는 문제를 막습니다.
OCR 과제에서는 이 한 줄이 체감되는 차이를 만듭니다.

---

## 3.9 모델 버전업 경로

| 단계 | 모델 | VRAM(4bit) | 근거 |
|---|---|---|---|
| 이전 | `qwen2.5-vl-7b` | ~6GB | 기준선 |
| **기본값** | **`qwen3-vl-8b`** | ~7GB | OCR 지원 언어 10→39개, **저조도·블러·기울어짐에 강함**, DeepStack이 ViT의 다층 특징을 融合해 미세 디테일 강화 |
| 상위 | `qwen3-vl-32b` | ~20GB | 더 강하지만 다운로드·추론 비용이 큼 |
| 대안 | `qwen3-vl-30b-a3b` | ~18GB | MoE(활성 3B)라 추론이 빠름. 단 VRAM은 30B분 필요 |
| 저사양 | `qwen3-vl-4b` | ~4GB | 3B 대비 업그레이드 |

Qwen3-VL을 1순위로 둔 이유는 크기가 아니라 **개선 내용이 이 대회의 실패 모드와 정확히 겹치기** 때문입니다.
휴대폰으로 찍은 간판·메뉴판은 흔들리고, 기울어지고, 조명이 고르지 않습니다.
Qwen3-VL이 명시적으로 개선했다고 밝힌 지점이 바로 그것입니다.

### 한국어 특화 모델은 왜 1순위가 아닌가

"한국어 데이터니까 한국어 특화 모델"은 직관적이지만, 근거가 반대 방향을 가리킵니다.
한국어 멀티모달 벤치마크(KorMedMCQA-V)에서 한국어 특화 최고 모델인
**VARCO-VISION-2.0-14B가 43.2%** 인 반면 **Qwen3-VL-32B-Thinking은 83.7%** 였습니다.
한국어 특화는 *생성 품질과 문화적 맥락*에서 강점이 있지만,
**이 대회에 필요한 건 글자를 읽는 능력과 4지선다 추론**이고 그건 대형 다국어 모델이 앞섭니다.

다만 이건 다른 벤치마크의 결과이므로, **반드시 본인 검증 세트로 직접 비교하세요.**
11번 셀의 후보 비교 장치가 정확히 그 용도입니다.

### 고려했으나 권장하지 않는 것

- **Thinking 계열** (`*-Thinking`) — 추론 과정을 먼저 생성하므로 **로짓 스코어링이 깨집니다.**
  쓰려면 파이프라인을 생성 기반으로 되돌려야 하는데, 그러면 파싱 실패 문제가 돌아옵니다.
- **전용 OCR 모델 단독 교체** (PaddleOCR-VL 등) — 문서 파싱에 최적화되어 있어
  4지선다 추론 능력이 없습니다. 다만 **보조 신호로는 유망합니다** (아래 8절 참고).
- **InternVL / VARCO 등 타 계열로 교체** — 전처리 API가 달라 `MODEL_ZOO`에 바로 넣을 수 없습니다.
  어댑터가 필요하며, 교체보다는 **앙상블용 다양성 확보**로 쓰는 편이 낫습니다.

---

## 4. 과적합 방지 — 4중 장치

사용자 요구사항이 "**과적합이 발생하지 않는 한도 내에서** 최고 점수"였으므로,
파인튜닝이 오히려 해로울 가능성을 **구조적으로** 차단했습니다.

| 장치 | 내용 |
|---|---|
| **① 격리된 검증 세트** | `train`을 층화추출로 분리. 학습·의사라벨 어디에도 절대 포함되지 않습니다. |
| **② 정확도 기준 best 체크포인트** | loss가 아니라 **검증 정확도**가 최고인 시점의 LoRA 가중치를 메모리에 보관하고 학습 종료 후 복원합니다. |
| **③ 얼리스토핑** | 연속 2회 검증에서 개선이 없으면 즉시 중단. 에폭은 1~2회로 제한. |
| **④ 최종 후보 자동 선택** | `zeroshot` / `finetuned` / `ensemble`을 검증 세트에서 겨루게 하고 이긴 쪽으로 제출. **파인튜닝이 제로샷을 이기지 못하면 파인튜닝 결과를 버립니다.** |

**④의 마진 규칙**이 핵심입니다. 검증 400문항에서 표준오차는 약 ±2.5%p이므로,
`+0.5%p` 이상 **확실히** 이길 때만 복잡한 후보로 교체하고 동점·박빙이면 단순한 쪽(`zeroshot`)을 택합니다.
후보를 3개로 못 박고 마진을 둔 덕분에 **검증 세트 자체에 과적합되는 것까지** 막습니다.

추가로 학습 데이터의 정답 분포를 사전확률로 약하게 반영하는 보정
(`최종점수 = log(모델확률) + α·log(정답분포)`)을 두었는데,
**α 역시 검증 세트로 고릅니다.** 효과가 없으면 `α=0`이 선택되어 아무 변화가 없으므로
**손해 볼 여지가 없는** 설계입니다.

---

## 5. 실행 방법

### Colab
1. 런타임 → 런타임 유형 변경 → **GPU**
2. 0번 셀 실행 → **런타임 다시 시작**
3. 1번 셀의 `DATA_ROOT` / `UNZIP_FROM`을 본인 경로로 수정
4. 위에서부터 순서대로 실행 → `submission.csv` 생성

### 로컬 (RTX 5060 Ti 등)
`UNZIP_FROM = None`으로 두고 `DATA_ROOT`만 데이터 폴더로 지정하면 됩니다.

### 실행 시간이 부담될 때
- `CFG['run_finetune'] = False` → **제로샷 + 고해상도 + TTA만으로도** 베이스라인보다 크게 앞섭니다
- `CFG['n_perm'] = 2` → 추론 시간 절반 (정확도는 보통 1~3%p 손해)

GPU를 자동 감지해 모델·해상도·배치를 프리셋으로 고르고, 추론 전 **속도를 실측해
전체 예상 소요 시간을 분 단위로 출력**합니다.

| 프리셋 | 기준 | 모델 | 추론 해상도 |
|---|---|---|---|
| `big` | VRAM ≥ 38GB | 7B (양자화 없음) | `1280×28×28` |
| `mid` | VRAM ≥ 21GB | 7B 4bit | `1280×28×28` |
| `small` | VRAM ≥ 14GB | 7B 4bit | `1024×28×28` |
| `tiny` | 그 이하 | 3B 4bit | `1024×28×28` |

---

## 6. 산출물

| 파일 | 설명 |
|---|---|
| `submission.csv` | 제출 파일. `sample_submission.csv`가 있으면 **id 순서·구성을 그대로 맞춥니다** |
| `report.json` | 모델/해상도/TTA/검증 정확도/최종 선택/학습 곡선 요약 — 발표·문서용 |
| `lora_best/` | best 검증 정확도 시점의 LoRA 어댑터 + 프로세서 |

제출 파일은 저장 직후 **컬럼 구성, `a~d` 이외 값 유무, id 중복, 행 수**를 `assert`로 검증합니다.

---

## 7. 재현성

- `seed=42`를 `random` / `numpy` / `torch` / `cuda`에 모두 고정
- 검증 분리는 정답 분포를 유지하는 **층화추출**, 시드 고정
- 학습 중 보기 셔플도 `(seed, epoch, index)` 기반 **결정적** 난수
- 추론은 `do_sample=False` + 로짓 스코어링이므로 **완전 결정적** (같은 입력 → 같은 출력)
- 라이브러리는 GitHub 최신 소스가 아니라 **동작이 검증된 릴리스 버전 범위**로 설치

---

## 8. 더 올리려면 (우선순위 순)

1. **`infer_max_tokens`를 더 올린다** (`1280 → 1600 → 2048`). OOM만 안 나면 거의 항상 이득 — 가성비 1위
2. **`CFG["model_key"]`를 `qwen3-vl-32b`로** — VRAM 24GB+ 확보 시
3. **큰 GPU에서 `load_4bit=False`** — 양자화는 정확도를 조금 깎습니다
4. **`n_perm`은 4 유지** — 1로 줄이면 보통 1~3%p 손해
5. **에폭 2회**로 늘리고 학습 곡선 확인 — 계속 오르면 데이터가 더 필요, 꺾이면 이미 충분
6. **`dev_min_agree`를 3으로** 낮춰 의사라벨 확대 (검증 정확도가 떨어지면 되돌리기)
7. **다중 해상도 앙상블** — `1024`와 `1600`으로 두 번 돌려 확률 평균 (작은 글씨/큰 간판 동시 커버, 시간 2배)
8. **서로 다른 계열 모델 앙상블** — `predict_probs`가 정규화된 `[N,4]` 확률을 돌려주므로
   **다른 모델의 출력을 그대로 평균**할 수 있습니다. 같은 계열보다 다른 계열(Qwen + InternVL 등)이
   앙상블 이득이 큽니다. 11번 셀의 후보 비교 틀을 그대로 확장하면 됩니다.
9. **하이브리드 OCR 보조 신호** — PaddleOCR(한국어 지원, 간판·거리뷰 포함)로 이미지의 글자를 먼저 뽑아
   프롬프트에 `[이미지에서 읽은 텍스트: ...]`로 넣어주는 방식. VLM이 놓친 작은 글씨를 전용 OCR이
   잡아주는 경우가 있고, **구조가 완전히 다르므로 앙상블 다양성이 큽니다.**
   단, 잘못 읽은 텍스트가 모델을 오도할 수 있으니 **반드시 검증 세트로 확인 후 채택**하세요.

### 하지 말아야 할 것
- ❌ 검증 정확도를 보며 하이퍼파라미터를 수십 번 바꾸기 → **검증 세트 과적합**
- ❌ 에폭 5~10회 → 4지선다 한 글자 예측은 금방 암기합니다
- ❌ `test` 이미지를 학습에 사용 → **규정 위반**

---

## 9. 검증한 내용

`tests/test_logic.py`는 GPU·모델 없이 순수 로직만 검증합니다 (`numpy`만 필요).

```
[1] 보기 순환 치환 TTA 매핑        — 자리 배치 완전 상쇄 / 강한 편향에서도 정답 복원 / TTA 미적용 시 오답 재현
[2] 라벨 마스킹                    — 정답 글자+종료토큰만 학습, 헤더 중복 시 마지막 기준
[3] dev 응답 → a~d 변환            — 16개 케이스(글자·번호·본문·오타·무응답·판독불가) + 다수결 게이트
[4] 사전확률 보정                  — 정규화 유지, α=0 무변경, 해로울 때 α=0 자동 선택
[5] 최종 후보 선택 마진 규칙        — 박빙이면 단순한 쪽, 파인튜닝 실패 시 자동 폐기
[6] 학습용 보기 셔플 증강          — 셔플 후 정답 정합성 500회, 에폭별 다른 순열
[7] 모델 교체(MODEL_ZOO)          — transformers 버전 폴백, 토큰→픽셀 환산, px_unit 미갱신 시 손실 검출
[8] assistant 헤더 자동 추출       — ChatML/Llama3 템플릿 모두에서 정확 추출, 마스킹 경계 검증
```

추가로 노트북 자체에 대해 전 코드 셀의 **문법 검사**와
**셀 간 참조 해석 검사**(뒤 셀에서 정의되는 이름을 앞 셀이 쓰지 않는지)를 통과시켰습니다.

> **주의:** 이 저장소에는 대회 데이터와 GPU가 없어 **모델을 실제로 학습·추론한 결과는 없습니다.**
> 위 테스트는 로직 정확성 검증이며, 리더보드 점수는 실제 환경에서 노트북을 실행해야 확인됩니다.
> 본문의 개선 효과 설명은 설계 근거이고, 실제 상승폭은 3.4절·6절의 검증 출력으로 직접 확인하실 수 있습니다.

---

## 참고 자료

- [Qwen3-VL (GitHub)](https://github.com/QwenLM/Qwen3-VL) — 모델 클래스, transformers 요구 버전, OCR 39개 언어 확대
- [Qwen3-VL Technical Report](https://arxiv.org/pdf/2511.21631) — DeepStack, Interleaved-MRoPE
- [Qwen/Qwen3-VL-8B-Instruct](https://huggingface.co/Qwen/Qwen3-VL-8B-Instruct)
- [KorMedMCQA-V](https://arxiv.org/html/2602.13650) — 한국어 특화 VLM vs 대형 다국어 VLM 비교
- [VARCO-VISION-2.0 Technical Report](https://arxiv.org/html/2509.10105)
- [InternVL3.5](https://arxiv.org/pdf/2508.18265)
- [PaddleOCR-VL](https://paddlepaddle.github.io/PaddleOCR/main/en/version3.x/algorithm/PaddleOCR-VL/PaddleOCR-VL.html) — 109개 언어(한국어 포함), 장면 텍스트
